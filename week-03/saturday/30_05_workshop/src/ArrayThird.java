public class ArrayThird {
    public static void main(String[] args) {

        int[] numbers = new int[4];
//int[] numbers = {4, 5, 6, 7};
        numbers[0] = 4;
        numbers[1] = 5;
        numbers[2] = 6;
        numbers[3] = 7;
        System.out.println(numbers[2]);
    }
}
// - Create an array variable named `numbers`
//   with the following content: `[4, 5, 6, 7]`
// - Print the third element of `numbers`





        /*        pole(pocitadlo);

        a = sin(3.6);

        a = 6;

        b = dvojnasobek(7);
        println(dvojnasobek(9));
        putblic staitic void pole ( int[] celeCisla)
        java.util.Arrays.binarySearch(cisla, 2);
        ArrayPolarkac16.dvojnasobek(3)

    }


    public static int dvojnasobek(int parametr) {
        return parametr*2;*/

        /*
        int[] anArray = new int[] {1, 2, 3, 4, 5};

        List<Integer> aList = new ArrayList<>();
        for (int element : anArray) {
        aList.add(element);

    //SORTINTG

        int[] anArray = new int[] {5, 2, 1, 4, 8};

        for (int i = 0; i < anArray.length; i++) {
        if (anArray[i] == 4) {
        System.out.println("Found at index " + i);
        break;

     //Method binary
     int[] anArray = new int[] {1, 2, 3, 4, 5};
    int index = Arrays.binarySearch(anArray, 4);
    System.out.println("Found at index " + index);

    //Concatenating Arrays
    int[] anArray = new int[] {5, 2, 1, 4, 8};
    int[] anotherArray = new int[] {10, 4, 9, 11, 2};

    int[] resultArray = new int[anArray.length + anotherArray.length];

    for (int i = 0; i < resultArray.length; i++) {
    resultArray[i] = (i < anArray.length ? anArray[i] : anotherArray[i - anArray.length]);
}
    }
}
    */

